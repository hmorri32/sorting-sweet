function divideAndConquer(leftArray, rightArray){
  var finished = [];


}



function mergeStuff(stuff){
  const length = stuff.length;
  if(length < 2) {
    return stuff;
  }
  var halfStuff  = Math.floor(length/2),
  var leftArray  = stuff.slice(0, halfStuff),
  var rightArray = stuff.slice(halfStuff);

}
