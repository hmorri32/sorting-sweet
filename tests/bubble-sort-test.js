import { expect } from 'chai';
import bubbleSort3 from '../scripts/bubble-sort'

describe('bubble sort tests maaannnnn', () => {
  it('should return an array', () => {
    expect(Array.isArray(bubbleSort3([], 'hello'))).to.be.true
  })
  
})



console.log('suh')
